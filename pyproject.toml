[build-system]
requires      = ["hatchling"]
build-backend = "hatchling.build"

[project]
name            = "tubeless"
version         = "0.1.0"
description     = "Fetch a YouTube video's transcript and summarize it with an LLM."
readme          = "README.md"
requires-python = ">=3.10"
dependencies    = [
    "youtube-transcript-api>=1.0",
    "openai>=1.0",
    "requests>=2.31",
]

[project.optional-dependencies]
# Claude is the second backend; its SDK is imported lazily, so it is optional --
# an OpenAI-only user need not install it (pip install tubeless[anthropic]).
anthropic = ["anthropic>=0.40"]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
    "anthropic>=0.40",
]

[project.scripts]
tubeless = "tubeless.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/tubeless"]

[tool.ruff]
line-length    = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
